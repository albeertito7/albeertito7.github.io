# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# gem "rails"

gem "github-pages", "209", group: :jekyll_plugins

# If using a Windows machine from the structure of the filepath.
# The `tzinfo-data` gem is a little picky about working on Windows.
# If you are using Ruby on Rails and your project was created on Windows, then
# you may find that there's is already a line in your Gemfile like the following:
##gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw]
# Moreover, if you are using a 64-bit version of Ruby on Windows, then
# add `:x64_mingw` to the list of platforms resulting as follows:
gem 'tzinfo-data', platforms: [:mingw, :mswin, :x64_mingw]

#gem "jekyll-swiss", "~> 1.0"